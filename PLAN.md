# TPIアセスメントWebアプリケーション 開発計画書

## 1. 概要

TPI NEXT®および Agile TPI のフレームワークに基づき、開発プロジェクトのテスト（品質保証）プロセス成熟度を診断するクライアント向けWebアプリケーションを開発する。現行、診断はExcel（VBAマクロ）ベースのツールで実施されており、本プロジェクトはこれをWebアプリケーションとして移植し、UXを改善するものである。

## 2. 背景・目的

- 現行の`TPI_NEXT_tool_v2.1.2_0-Nippon-one-list_0.xls`、`Agile-TPI-tool-Japanese-v1.0.xls`はExcel VBAで動作しており、配布・利用のハードルが高い（マクロ有効化、Excel環境依存等）。
- ブラウザで完結する診断フローを提供し、結果の可視化（マトリクス図・スパイダーグラフ）とPDF出力までをシームレスに行えるようにする。

## 3. スコープ

### 3.1 対象
- トップ画面（フレームワーク選択：TPI NEXT / Agile TPI）
- 診断画面（チェックポイントへの回答）
- 診断結果画面（成熟度マトリクス、スパイダーグラフ、短評、PDF出力）

### 3.2 対象外（初期リリースでは扱わない）
- ユーザー認証・アカウント管理
- サーバー・DBへの診断結果の保存（バックエンドなしの方針のため。ブラウザのlocalStorageへの保存は§5.3参照）
- 複数の診断結果の履歴管理（フレームワークごとに最新の回答のみ保持）
- 複数人での共同回答・コメント機能
- 多言語対応（原則日本語のみ）

## 4. 参考資料（Excelツール）の分析結果

### 4.1 TPI NEXT（`TPI_NEXT_tool...xls`）

| シート | 内容 |
|---|---|
| `All Checkpoints` | 16のキーエリア×Controlled/Efficient/Optimizingの3段階、計240行（実チェックポイントは約190項目） |
| `Test maturity matrix` | キーエリアごとの成熟度マトリクス（Initial/Controlled/Efficient/Optimizingの4段階×クラスタ文字A〜M） |
| `assessmentData` / `baseData` | 各チェックポイントの回答とクラスタ文字（重要度H/N/L別）の対応表 |
| `Cluster A`〜`Cluster M` | 各クラスタ文字達成に必要な具体的チェックポイント群（H/N重要度別） |
| `Config` / `Language` | 表示設定・多言語ラベル |

**キーエリア（16項目）**：利害関係者のコミットメント／関与の度合い／テスト戦略／テスト組織／コミュニケーション／レポート／テストプロセス管理／見積もりと計画／メトリクス／欠陥管理／テストウェア管理／手法の実践／テスト担当者のプロ意識／テスト設計／テストツール／テスト環境

**回答形式**：各チェックポイントは「満たしている／満たしていない／該当なし」の3択（凡例：green＝met, red＝not met, gray＝not applicable）。

**成熟度算出の考え方**：各キーエリアには重要度設定（H＝高／N＝標準／L＝低、デフォルトはN）があり、この重要度に応じた閾値表（`baseData`）を使って「回答済みチェックポイント数」から到達クラスタ文字（A〜M）を決定し、それをInitial〜Optimizingの4段階マトリクスに配置している。

### 4.2 Agile TPI（`Agile-TPI-tool-Japanese-v1.0.xls`）

| シート | 内容 |
|---|---|
| `All checkpointsMaster` | 全108チェックポイント（`Y`/`NA`/`N`の3択で回答、P=Product/T=Team/O=Organization の3カテゴリに分類） |
| `Test maturity matrix` | 「Professional／Team／Organization」の3軸マトリクス（TPI NEXTの4段階ステージとは異なる構成） |
| `graphData` | キーエリアごとのY/NA/N集計とパーセンテージ（スパイダーグラフの元データ） |
| `baseData` | チェックポイントとクラスタ文字の対応 |

キーエリア名はTPI NEXTと同じ16項目を踏襲しているが、マトリクスの軸構成（4段階 vs Professional/Team/Organizationの3軸）が異なる点に注意。

## 5. 画面構成・機能要件

### 5.1 トップ画面
- 「TPI NEXT」「Agile TPI」いずれかを選択するボタン／カード
- 選択に応じて `#/tpi-next` または `#/agile-tpi` の診断画面へ遷移

### 5.2 診断画面
- 選択したフレームワークに応じ、キーエリアごとにグルーピングされたチェックポイント一覧を表示
  - TPI NEXT：キーエリア→Controlled/Efficient/Optimizingの段階→チェックポイント、の階層表示
  - Agile TPI：キーエリア→チェックポイント（P/T/Oカテゴリのラベル付き）
- 各チェックポイントは「満たしている／満たしていない／該当なし」の3択（ラジオボタン）
- 「該当なし」の右側に、チェックポイントごとのメモ欄（テキストエリア）を配置。1行全角20文字程度・5行程度の高さで固定表示し、それを超える分は縦スクロールで見せる（自動リサイズはしない）。回答と同様にlocalStorageへ保存し、任意入力（バリデーション対象外）
- 画面下部に「診断する」ボタン
- 未回答チェックポイントがある状態で「診断する」を押すとバリデーションエラー：該当項目に背景色を付けてスクロール誘導
- 進捗表示（回答数／全体数）は要検討
- 画面上部に「回答をクリアする」ボタン。押下すると確認モーダル（タイトル「回答をクリアしますか？」、本文「現在の回答・メモがすべて削除され、画面が初期状態に戻ります。この操作は取り消せません。」、「キャンセル」／「削除する」の2択）を表示。「削除する」で今開いているフレームワークの回答・メモのみを削除（もう一方のフレームワークの下書きには影響しない）

### 5.3 診断結果画面
- **①成熟度マトリクス**：キーエリア×段階（TPI NEXTはControlled/Efficient/Optimizingの3段階、Agile TPIは3軸）をグリッド表示し、達成セルを緑、未達成を赤、対象外をグレーで着色。TPI NEXTのInitial列（チェックポイントを持たず全プロジェクトが到達済みの基礎段階）は表示上の情報価値が低いため列自体を非表示にしている
- **②スパイダーグラフ**：16キーエリアを軸とするレーダーチャート。現在の到達度（%）と満点ラインを重ねて表示
- **③キーエリア別達成率**：キーエリア×段階（TPI NEXTはControlled/Efficient/Optimizing）／軸（Agile TPIはProfessional/Team/Organization）ごとの達成率に加え、一番右の列に全軸合計の達成率（②のスパイダーグラフと同じ値）を表示。各セルはパーセンテージに続けて分子/分母（例：`75%(3/4)`）を併記し、分母0件（該当なし・未回答のみ）の場合は`0%(0/0)`と表示する
- **④短評**：テキストエリアとして表示し、診断結果に応じたルールベースの文言をデフォルトで出力する（`js/review.js`、Excel版には存在しない新規機能）。「前回結果画面を表示した時点の回答」をlocalStorageに保存しておき（`tpiAssessmentTool.review.v1`）、次に結果画面を表示する際、回答が前回と完全に一致していれば保存済みの内容（編集済みの場合も含む）をそのまま復元し、1件でも変わっていれば新しいデフォルト文言を生成し直す。誤って診断画面に戻ってすぐ結果画面に戻ってきただけでは編集内容が消えないようにするための仕様（比較対象は回答のみで、メモの変更は対象外）。「回答をクリアする」実行時はこのスナップショットも削除する
  - **TPI NEXT**：CONTROLLED→EFFICIENT→OPTIMIZINGの順に「そのステージの全チェックポイントが『満たしている』または『該当なし』か」を判定し、最初に未達成だったステージ向けの導入メッセージ＋「満たしていない」チェックポイントのクラスタ文字（A→M、既定の優先度=N）昇順トップ5（同率5位はすべて含める）を表示。全ステージ達成時は固定の称賛メッセージのみ
  - **Agile TPI**：ステージの概念がないため、「満たしていない」チェックポイントをクラスタ文字昇順でトップ5（同率5位含む）表示。全チェックポイント達成時は固定の称賛メッセージ（ユーザー仕様に明記がなかったため、TPI NEXTの全達成メッセージに準じて独自に追加）
  - 優先チェックポイント1件の表記形式：1行目`[クラスタ文字][キーエリア名]`（Agile TPIは`[クラスタ文字][軸名][キーエリア名]`）、2行目に全角スペース1つ+チェックポイント本文
- PDF出力ボタン：上記①②③④をレイアウトしたPDFをブラウザ内で生成しダウンロード
- 診断結果はサーバー・DBには一切送信・保存しない。回答内容はブラウザの**localStorage**にフレームワークごとに保存し、ブラウザを閉じても回答が残る（端末・ブラウザ単位。他端末との同期や共有はできない）。結果画面は毎回localStorageの回答から算出し直すため、結果そのものを別途保存する必要はない。この方針を明示的に残す手段としてPDF出力も用意する

## 6. 成熟度算出ロジック

- Excel(VBA)版と**完全一致**することを目標とする
- `baseData`（H/N/L別の閾値表）、`Cluster A〜M`（クラスタ文字ごとの必須チェックポイント群）を構造化データ（JSON）として移植し、**ブラウザ内JS**で計算する
- 実装後、Excel版に同一回答パターンを入力した結果とJS側の算出結果を突合するテストを必須工程とする

## 7. 非機能要件

- 対応ブラウザ：モダンブラウザ（Chrome/Edge/Safari最新版を想定、要確認）
- レスポンシブ対応の要否（PC想定か、タブレット/スマホも含むか）は要検討
- アクセシビリティ：フォーム未回答時の視覚的フィードバックに加え、スクリーンリーダー対応の要否は要検討

## 8. データモデル

サーバー用スキーマは不要。localStorageに保存するのは回答とメモのみとし、マトリクス・達成率・アドバイスは表示のたびに算出する（保存データと算出ロジックの二重管理を避けるため）：

```
// localStorageキー「tpiAssessmentTool.answers.v1」：フレームワークごとの回答
{
  "tpi-next": { [checkpointId]: "met" | "not_met" | "n_a" },
  "agile-tpi": { [checkpointId]: "met" | "not_met" | "n_a" }
}

// localStorageキー「tpiAssessmentTool.notes.v1」：フレームワークごとのメモ（任意入力）
{
  "tpi-next": { [checkpointId]: string },
  "agile-tpi": { [checkpointId]: string }
}

// localStorageキー「tpiAssessmentTool.review.v1」：フレームワークごとの短評＋保存時点の回答スナップショット
{
  "tpi-next": { text: string, answers: { [checkpointId]: "met" | "not_met" | "n_a" } },
  "agile-tpi": { text: string, answers: { [checkpointId]: "met" | "not_met" | "n_a" } }
}
```

## 9. 技術スタック

- **バックエンドなし・DBなし**。`line-stamp-tool`と同様の思想（ビルド不要・依存インストール不要）を踏襲するVanilla JS + Canvas/SVG構成を候補とする
- チェックポイントデータ（TPI NEXT 約190項目、Agile TPI 108項目）と閾値表（`baseData`／`Cluster A〜M`）は静的JSONとして同梱し、フロントJSがロードして計算
- PDF出力はクライアントサイドライブラリ（例：jsPDF等）で実現

## 10. 開発フェーズ（案）

1. ✅ チェックポイントデータのマスタ化（Excelから抽出したJSON化）
2. ✅ 成熟度算出ロジックの実装・検証（達成率算出式は要実機検証、[ALGORITHM.md](ALGORITHM.md)参照）
3. ✅ トップ画面／診断画面の実装
4. ✅ 結果画面（マトリクス・スパイダーグラフ）の実装
5. ✅ 短評・アドバイス機能（ルールベース）の実装
6. PDF出力機能の実装
7. 総合テスト（Excel版との結果突合、バリデーション確認）

## 11. リスク・課題

- **成熟度算出ロジックの再現精度**：VBA解析の結果、チェックポイント→クラスタ文字の対応（マトリクスのセルラベル）は`baseData`シートの静的なコピーであることが判明し、`data/tpi-next/threshold-table.json`として完全再現済み（詳細は[ALGORITHM.md](ALGORITHM.md)）。一方、スパイダーグラフの達成率(%)算出式はExcel/LibreOffice不在の開発環境のため実際の突合検証ができておらず、構造的根拠に基づく推定値である点は未解決のリスクとして残る
- **短評・改善アドバイス機能**：元のExcelツールに存在しない新規要件のため、ルール（どのスコア帯でどんな助言を出すか）の設計を別途詰める必要がある
- **Agile TPIのマトリクス軸の違い**：TPI NEXTの4段階（Initial/Controlled/Efficient/Optimizing）と異なり3軸（Professional/Team/Organization）であるため、共通コンポーネント化が難しい可能性
- **完全クライアントサイド構成のテスト**：算出ロジックの正しさを保証するテストは、ブラウザ実行のJSユニットテストで行う必要がある

## 12. 確認事項（未解決）

- 対応ブラウザ・レスポンシブ対応の範囲
- 短評・改善アドバイスのルール内容（スコア帯とメッセージの具体的な対応表）
- スパイダーグラフの達成率(%)算出式の実機検証（Excel/LibreOfficeで既知の回答パターンを入力し、表示値と`js/maturity.js`の算出結果を突合する。開発環境にExcel/LibreOfficeがないため未実施。詳細は[ALGORITHM.md](ALGORITHM.md)の「未検証事項」）

## 決定事項サマリー（本計画書確定までの経緯）

| 項目 | 決定 |
|---|---|
| 算出ロジックの再現精度 | Excel(VBA)版と完全一致を目指す |
| 改善アドバイスの生成方法 | ルールベース（固定文言） |
| アーキテクチャ | バックエンド・DBなし、完全クライアントサイド |
| 回答の永続化 | ブラウザのlocalStorageに保存し、ブラウザを閉じても回答が残るようにする（サーバー・DBへは送信しない） |
| リポジトリ | line-stamp-toolとは別の新規リポジトリ（`tpi-assessment-tool`） |
| GitHub公開範囲 | Private |
