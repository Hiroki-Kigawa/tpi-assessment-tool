# 成熟度算出ロジック — 実装メモ

このドキュメントは、元Excel(.xls)のVBAマクロ・シート構造を解析して得た知見と、
それに基づく `js/maturity.js` の実装根拠をまとめたもの。実装前に必ず参照すること。

## 調査方法

`oletools.olevba` で両ファイルからVBAソースを抽出し、シート構造（`xlrd`で読取）と突き合わせて解析した。
Excel/LibreOfficeが利用できない環境のため、**実際にセルを再計算させての突合検証は行っていない**（詳細は「未検証事項」参照）。

## TPI NEXT

### チェックポイント → クラスタ文字（成熟度マトリクスのセルラベル）

- VBA `Blad38.updateMaturityMatrix`（旧: 該当モジュール）は、選択中の重要度(H/N/L)に応じて
  `<キーエリア番号>_<H|N|L>_Clusters` という名前付き範囲から**クラスタ文字の配列をそのままマトリクスのセルへコピー**しているだけで、動的な計算は行っていない。
- この配列の実体は `baseData` シート（16キーエリア×3重要度=48行）。各行は、そのキーエリアのチェックポイントを元シート順（Controlled→Efficient→Optimizingの順）に並べたときの、位置ごとのクラスタ文字である。
- したがって「チェックポイントiがどのクラスタ文字に属するか」は **`baseData`の値をそのまま使えば完全再現になる**（VBAは単なるコピー処理のため）。これを `data/tpi-next/threshold-table.json` に構造化した（`scripts/extract_maturity_rules.py`）。
- セルの色（達成=緑／未達成=赤／該当なし=グレー）は、チェックポイント自体の回答（満たしている/満たしていない/該当なし）をそのまま反映するだけで、クラスタ文字とは独立している。集計や閾値判定は不要。
- 重要度(H/N/L)の既定値は全16キーエリートも **"N"（標準）**（`Test maturity matrix`シートの初期状態で確認済み）。

### スパイダーグラフの達成率（％）

- TPI NEXT側には、Agile TPIの`graphData`に相当する専用の集計シートが見つからなかった（`xlrd`で列挙されるシート一覧に存在せず、`baseData`という文字列も検出されず）。
- 一方、バイナリ内に `Spider` という文字列が複数検出されており、**非表示（very hidden）のチャートシートとしてスパイダーグラフ自体は存在する**と推測される。そのグラフのデータソース（チャートのSERIES式）は`xlrd`では抽出できなかった。
- Agile TPIの`graphData`シートの列構成（`perc.` / `'Y'` / `'NA'` / `'N'` / `total`）から、**達成率 = 満たしている数 ÷ (満たしている数 + 満たしていない数) × 100**（該当なし・未回答は分母から除外）という式が最も自然であり、これをTPI NEXT・Agile TPI共通の算出式として採用した。

## Agile TPI

### チェックポイント → クラスタ文字

- Agile TPIには重要度(H/N/L)の選択機構がない（該当するVBAコードが存在しない）。
- `All checkpointsMaster`シートの2列目に、チェックポイントごとの単一のクラスタ文字が直接格納されている。これをそのまま`data/agile-tpi/checkpoints.json`の`cluster`フィールドとして保持済み。

### Professional / Team / Organization 軸

- VBA `Blad43.setNbrOffCheckpoints` が、チェックポイントIDの文字列に `.1.` / `.2.` / `.3.` が含まれるかで
  P（Professional）/ T（Team）/ O（Organization）を分類し、達成数を集計していることを確認した。
- チェックポイントIDは `<キーエリア番号>.<軸番号>.<連番>` の形式であるため、実装では文字列判定ではなく
  IDを`.`で分割して2番目の要素を直接使用している（`scripts/extract_maturity_rules.py`の`AXIS_BY_SUBGROUP`）。
- 抽出結果は P=44 / T=32 / O=32 で、元ファイルの`graphData`シートの合計値（`Category 'P' checkpoints: 44` 等）と完全に一致することを確認済み。

## 検証済み事項

`scripts/validate_maturity_data.py` で以下を自動検証している（`python3 scripts/validate_maturity_data.py`で実行可能）：

- `threshold-table.json`の各行のチェックポイント順序が、元シートの並び順（`checkpoints.json`の順序）と一致すること
- 各キーエリア×重要度のクラスタ文字列が単調非減少であること（TPI NEXTの成熟度モデル上の整合性）
- 達成率算出式の境界値（全問met→100%、全問not_met→0%、該当なしのみ→0%、半々→50%）
- Agile TPIのP/T/O軸分布・総チェックポイント数が元ファイルの集計値と一致すること

## 未検証事項（要フォローアップ）

- **達成率(%)の算出式**：構造的な根拠に基づく推定であり、元ファイルのチャートSERIES式そのものを確認したものではない。Excel（またはLibreOffice）で実際にファイルを開き、既知の回答パターンを入力してグラフの表示値と突き合わせる検証が望ましい。
- **TPI NEXTのスパイダーグラフのデータソース**：`baseData`/`graphData`相当のシートが見つからなかったため、16キーエリアの達成率をどのシート/数式から取得しているかは未確認（本実装は独自に算出）。
- 本リポジトリの開発環境にはExcel・LibreOfficeが存在しないため、上記はこのセッションでは検証できなかった。ユーザー側でExcel等が使える環境があれば、既知の回答パターンでの突合を推奨する。
